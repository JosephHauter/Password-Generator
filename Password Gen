import random
import string

x = int(input("Enter the amount of minimum characters: "))

def passwordGenerator():
    password = ""

    for i in range(x):
        password += random.choice(string.printable)

    return (password)


print(passwordGenerator())
